# Fresnica Mobile Product Traceability Ledger

> Stage 0 stable product ledger. Source behavior comes from `origin/Stellar@stellar-migration`; security/business semantics come from `origin/fresnica`. Donor implementation is clean-room by default under `docs/provenance/README.md`.

| ID | Product behavior | Target owner | Decision | Provenance path | External/upstream dependency | Current maturity |
| --- | --- | --- | --- | --- | --- | --- |
| S01 | Create/import/watch-only/HD account entry | accounts/onboarding + Account/Signer | Adapt | clean-room | Create + Import + HD are merged and dual-platform aggregate accepted; repeatable required gate + accessibility closure remain | L3 / L4 partial |
| S02 | Account list/select/label/sort/hide/delete | accounts + Account | Adapt | clean-room | persistence lifecycle | L3 / L4 partial |
| S03 | Reveal mnemonic/secret/change passphrase | accounts/security + Signer/Backup | Adapt | clean-room | Fresnica reveal/reprotect/System Auth | L1/L2 partial |
| S04 | App lock/auth/security/privacy controls | security + Application Security | Adapt | clean-room / Fresnica security requirement for disable confirmation | generic System Auth challenge still missing | L3 partial/blocker |
| S05 | Home balances/availability/refresh/inactive/Friendbot | home + Balance/Network | Adapt | clean-room | Horizon; Friendbot Testnet only; dual-platform aggregate accepted; required gate + accessibility closure remain | L3 / L4 partial |
| S06 | Asset discovery/details/icons/risk | assets + Asset Discovery | Adapt | clean-room | TOML/catalog authority | L1 partial |
| S07 | Send/CreateAccount + memo families | send + Payment/Transaction | Adapt | clean-room | 2026-09-18 dual-platform aggregate covers memo families, passphrase fallback, System Auth success, submission outcomes and restart recovery; native iOS System Auth cancel remains unproven | L3 partial |
| S08 | M-address/muxed identity | send/request + Destination | Adapt | clean-room | shared destination contract required | L0 blocked |
| S09 | Request/QR/scan/share/deep link | request + Intent/system ports | Adapt | clean-room | SEP-7 + camera/share/deep-link | L0 |
| S10 | Path Payment strict send/receive swap | exchange + Swap/Transaction | Adapt | clean-room | shared Path Payment capability required | L1 mechanism only |
| S11 | Claimable balance list/risk/claim | claimables + capability | Adapt | clean-room | write semantics to define | L0 |
| S12 | Liquidity pool list/details/withdraw | liquidity + pool capability | Adapt | clean-room | shared LP write contract required | L1 read mechanism only |
| S13 | Activity list/filter/search/detail/participants/offline UX cache | activity + History | Adapt | clean-room | Horizon authority + replaceable network/account-scoped Realm cache | L3 partial |
| S14 | dApp catalog/search/category/icons | dapps + Catalog ports | Adapt | clean-room | `dapp.fchain.io` catalog/backend boundaries | L1 scaffold |
| S15 | Recent/disclaimer/custom URL | dapps + preferences | Adapt | clean-room | persistence + origin policy | L1 scaffold |
| S16 | dApp browser/origin/navigation lifecycle | dapps + platform/dapp-browser | Adapt | clean-room | WebView/origin security | L0 |
| S17 | dApp permissions/session/revoke | dapps/security + permission capability | Adapt | clean-room | permission contract/persistence | L0 |
| S18 | Freighter account/network/transaction requests | dapps + Transaction/Signing | Adapt | clean-room | permission + arbitrary reviewed transaction contract | L0 |
| S19 | SEP-53 message signing | dapps + Fresnica native | Adapt | clean-room | Native SDK 0.3.1 bridge available | L2 mechanism |
| S20 | Soroban auth-entry request | dapps + Fresnica native | Adapt | clean-room | RN secure delivery missing | L0 blocked |
| S21 | Browser commands/device/share/data bridge | dapps + controlled platform ports | Adapt | clean-room | per-command contract | L0 |
| S22 | Address book/destination picker | contacts + Contacts capability | Adapt | clean-room | local contacts policy | L0 |
| S23 | General settings/language/display/theme/feedback | settings + Preferences/locale/AppTheme | Adapt | clean-room | final visual/theme persistence | L2/L3 partial |
| S24 | Network list/switch/custom endpoint | settings + Network | Adapt | clean-room | Developer Mode auth/persistence | L1 |
| S25 | Developer settings/session logs/diagnostics | settings + Diagnostics | Adapt | clean-room | user-reachable export/policy | L1/L2 partial |
| S26 | Help/Credits/Terms/ChangeLog/browser | settings/support + system browser | Adapt | clean-room | legal/content inputs | L1 |
| S27 | AppState/privacy/clipboard/camera/files/share permissions | app/platform/system | Adapt | clean-room | OS lifecycle/permission APIs | L1 partial |
| S28 | External/hardware signer provider | accounts/security + external signer | Adapt | clean-room | verified Ledger BLE matrix; Fresnica prepare/apply | L2 seam only |
| S29 | Old Stellar/Xaman data behavior | app/persistence migration guard | Exclude old-data migration; Adapt fresh-start detection | clean-room | final app identity + legacy detection | L0 |
| S30 | Trustline Add/Set Limit/Remove | trustlines + Trustline/Transaction | Adapt | clean-room | Fresnica Trustline contract; 2026-09-20 post-#59 dual-platform aggregate accepted; required gate + reliable VoiceOver/focus-order depth remain | L3 / L4 partial |

## Delivery and acceptance map

This map assigns construction order without reducing final scope. An ID is complete only when its row-specific behavior and the execution-plan Feature Definition of Done both have current evidence.

| Target stage | Trace IDs | Acceptance anchor |
| --- | --- | --- |
| Stage 0 / 0A | S01–S30 | Every implementation PR names IDs; provenance path is recorded; donor-derived behavior has a clean-room behavior specification before code is written. Current target tree passes the fixed donor exact-blob/marker gate, and donor-informed presentation receives a separate human structure review; automated zero-collision is necessary but not sufficient. |
| Stage 2.5 | Current ledger-write IDs S07 and S30; mandatory later for the ledger-write portions of S10–S12 and S18 | **Passed 2026-09-10 for the shared recovery gate.** Pending submission is persisted before broadcast; reconciliation uses the original network-bound transaction hash; unresolved duplicates are blocked by network + account + economic intent with no time expiry. Payment and Trustline prove the shared implementation before another write type is added. |
| Stage 3 | S04, S23, S24, S25, S27 | Shared shell/theme/modal/error/diagnostic/lifecycle carriers exist; a minimal native-flow E2E harness proves they work across feature boundaries. |
| Stage 4 | S01–S08, S13, S30 and the local-wallet portion of S04/S23/S24/S27 | Account lifecycle, Home, Send, Trustline, Activity and core Settings meet their §9 flows, failure states, localization/accessibility and native/E2E evidence. S13 additionally proves network/account-isolated cache hydration, honest offline/last-updated presentation and Horizon revalidation without treating cache as complete history or resuming persisted cursors. Blocked memo/muxed/security semantics remain explicit, not silently excluded. S30 additionally proves Add/Set Limit/Remove, exact asset identity, pre-sign ledger revalidation and Stage 2.5 recovery. |
| Stage 5 | S09 plus the destination/deep-link portions of S08/S27 | QR, share, SEP-7, paste/scan and deep link converge on one typed parser/router and fail closed on network or asset mismatch. |
| Stage 6 | S10 | Strict-send and strict-receive quote/review/expiry/slippage/submit flows use the shared transaction and recovery pipeline after the upstream capability is authoritative. |
| Stage 7 | S14–S21 | Catalog, browser, origin policy, permissions, Freighter request matrix, SEP-53/Soroban and controlled system bridges are clean-room, revocable, auditable and routed through standard review/sign/submit. |
| Stage 8 | S03, S06, S11–S13, S22–S28 | Remaining asset, claimable, LP, contact, diagnostics, support, platform and external-signer behavior reaches the relevant §7/§9 contract. Hardware support is limited to combinations proven on actual devices. |
| Stage 8B | Backend-dependent portions of S14, S17, S24 and S27 | Versioned backend contracts and threat models exist; unavailable/not-configured/degraded states are honest and do not block unrelated local-wallet capability. |
| Stage 9 | S04, S24, S27, S29 and all release-critical IDs | Android identity is `com.fresnica.wallet`; the new iOS Bundle ID is frozen; legacy data is never silently adopted or erased; release signing, SBOM/provenance and critical native E2E matrix pass. |

## Stage 2.5 recovery evidence

- Initial recovery evidence head: `5db3610` (includes `043c947`, `3651aa7`, `b035037`, `6e50817`, `8f5bffd` and Horizon transport fix `eaa1e8c`).
- Merge-review closure: `8a3ff12` queues one trailing reconciliation when a trigger arrives during an active single-flight run; `f1c47c3` rejects any reconciliation outcome whose returned transaction hash differs from the persisted pending hash; `3801415` adds real Realm v2→v3 migration coverage and the pre-broadcast crash-window fail-closed test.
- Post-review local gate: `npm run check` 56 suites / 288 tests passed; `npm run test:realm`: 19/19; provenance/architecture/locale gates passed.
- Android real-Testnet process-death smoke: transaction `f4c319cbe5db754888a419af7311901331ebf1b5a9209d2dd7fff8cd057e1417` persisted uncertain under PID 7631, process was force-stopped, PID 7773 reopened the same Realm and reconciled the same network/account/source/hash to confirmed without signing or broadcasting again.
- Android and iOS current-head native runtime smoke both passed after the NetInfo dependency was integrated; iOS also completed a fresh simulator build.
- This evidence passed the S07/S30 **shared recovery gate only**. At the time of this Stage 2.5 evidence, both product rows remained `L3 partial` pending their later Stage 4 user-flow, failure, localization/accessibility and native/E2E acceptance; current maturity is recorded in the delivery map and the later per-slice aggregate evidence below.
- Future ledger-write slices S10–S12 and S18 must consume this recovery path; passing Stage 2.5 is not permission to implement them before their own Stage/Capability gates.

## Stage 3 product native-flow carrier evidence

- `npm run smoke:product-flow:android` is the canonical fresh-build carrier. It uses production `createAppServices` with an isolated Realm path, generated-mnemonic onboarding, programmatic low-level backup-state completion/bootstrap, the production `AppNavigator` main branch, Balance capability, and the Send product flow against Stellar Testnet. That direct completion is test infrastructure and does not count as user mnemonic-verification proof.
- Two independent Android runs on 2026-09-10 both reported `onboarding -> pending-mnemonic-backup -> ready`, active Balance reads, and successful Testnet writes. Transaction hashes: `5b606a230dfd30e4a378474b1774599c433778a0344122c752db8b060e5cd1d7` and `f3a2acf2751cee4d787fed3b248c70a4ce20d7bf3833f163c9bafedacf65bd50`. Each fresh source moved from `10000.0000000` to `9997.9999900` XLM.
- `npm run check` now validates the harness shell syntax plus its JS Prettier/ESLint gates without performing network writes. Full tap/visual/accessibility/System Auth device E2E remains per-slice acceptance work, so this closes the Stage 3 carrier requirement only.
- Detailed command, isolation, redaction, and scope notes: `docs/product-native-flow-smoke.md`.

## Stage 4 S02 account-label evidence

- Account label rename is implemented as a clean-room S02 sub-slice. The Account capability trims the requested label, treats an unchanged trimmed value as a no-op, and delegates only `label + updatedAt` persistence to the injected Account repository; account address/network/identity/sort/visibility and signer relationships are not rewritten.
- Memory and Realm implementations share the same repository contract; Realm integration proves the renamed label survives close/reopen. Missing account fails closed.
- Settings → Accounts → Account Detail exposes a localized edit-label modal. Save refreshes App bootstrap through the existing `onAccountsChanged()` path; failure remains in the modal with stable localized copy. Android emulator validation on 2026-09-11 proved `BeforeRename → BeforeRenameAfter` in Account Detail, the Accounts list, and again after force-stop/restart.
- This evidence closes **rename only**. Hide/show + local delete and Sort/reorder are closed by the separate evidence blocks below; aggregate S02 maturity is still reviewed independently.

## Stage 4 S02 hide/show and local-delete evidence

- Lifecycle branch `stage4/s02-account-lifecycle-2026-09-11` is based on `main` after PR #39 and adds only narrow local visibility/delete policy. The last visible account cannot be hidden; hidden records remain in Settings → Accounts and can be restored, while Home/account selection filters them out.
- Local delete is blocked while the target account has any unresolved `submitting`/`uncertain` submission. It delegates the actual mutation to the existing Account repository, preserving shared signers until their final account reference is removed. Deleting the true final account is allowed and the existing onboarding bootstrap resolves the empty repository to `onboarding`; deleting the last visible account while hidden records remain is blocked to preserve the main-shell invariant.
- Selected-account reconciliation uses stable `sortOrder → createdAt → id` ordering and chooses the next previously visible account after the current account is hidden or removed. Delete UI uses explicit second confirmation and states that only the local wallet record is removed, not the Stellar on-chain account. Navigation/bootstrap refresh happens only after repository deletion succeeds; failure remains on Account Detail.
- Implementation and acceptance target `a75914b25efb6959ca22b0e7582c96e21e8e5140`: `npm run check` **60/60 suites, 309/309 tests**, ESLint **0 errors / 23 warnings**, architecture/alias/provenance/locale gates passed; locale dictionaries share **122 keys**; donor gate scanned **359 files with 0 unapproved exact donor blobs**. `npm run test:realm` passed **23/23**. Remote CI, Realm Integration, Native Android Gate and Native Apple Gate were all green on that exact head.
- Android emulator native acceptance on 2026-09-11 proved hide/restore persistence across restart, last-visible Hide/Delete guards, unresolved pending deletion blocking, stable selected-account takeover, shared-signer retention until reference count reaches zero, final-account deletion into the existing Onboarding bootstrap, and a real Realm write-failure path that stays on Account Detail without a success refresh/navigation. This closes the **Hide/Delete sub-slice only**. Sort/reorder is covered separately below and does not alter the frozen lifecycle policy.


## Stage 4 S02 sort/reorder evidence

- Independent branch `stage4/s02-account-sort-2026-09-11` is based on merged `main@7ff333c` and changes only local display ordering / `sortOrder` semantics. It does not modify the frozen rename, hide/show or delete policies.
- Account ordering is centralized as `sortOrder → createdAt → id`. Adjacent move operations persist `sortOrder + updatedAt` only through one batch repository mutation; Realm resolves all records and writes them in one transaction. Missing-account failure therefore cannot partially reorder the wallet. New accounts append after the current maximum `sortOrder` instead of reusing zero.
- Settings → Accounts exposes neutral localized Move up / Move down controls. Hidden accounts remain manageable in the same local ordering and keep their hidden flag; Home continues to filter hidden accounts before applying the shared stable order. A still-visible current account keeps its selected identity during a live reorder.
- Implementation/native target `fc169947ddcf76634c7846f517afd1b003c18459`: local `npm run check` passed **61/61 suites, 318/318 tests**, ESLint **0 errors / 23 warnings**, architecture/alias/provenance/locale gates passed; locale dictionaries share **125 keys**; donor gate scanned **361 files with 0 unapproved exact donor blobs**. `npm run test:realm` passed **2 suites / 26 tests**, including atomic repository sort updates and close/reopen order persistence.
- Review accessibility closure started at `fc477902e5d41600d6aa2dc68a28054d46ac1620` with disabled visual/accessibility state, explicit failure announcement and style extraction. Final review-fix target `dcca4923ac978631f2d5101dbc8dee7b05105201` keeps `AccessibilityInfo.announceForAccessibility` as the single proactive screen-reader announcement path and removes the assertive live region from the same inline error to avoid duplicate TalkBack speech. `AccountsScreen.tsx`, `styles.ts` and `AccountsScreen.test.ts` are all Prettier-clean, and the minimal UI suite now also guards that the rendered error stays passive. Local `npm run check` passed **62/62 suites, 321/321 tests**, ESLint **0 errors / 23 warnings**; provenance scanned **363 files with 0 unapproved exact donor blobs**; `npm run test:realm` remains **2 suites / 26 tests**. The sort capability, Realm mutation, account-selection logic and frozen Rename/Hide/Delete implementation were unchanged by both review fixes.
- Android emulator acceptance on 2026-09-11 created disposable watch-only `SortB` / `SortC` records beside existing account `11`. Moving current `11` down changed the Settings order from `11, SortB, SortC` to `SortB, 11, SortC` while Home still reported `Current account 11`. Hiding `SortC` and moving it up produced `SortB, SortC, 11` without clearing the `Hidden` state; after force-stop/restart the same order and hidden state were restored. Test records were deleted afterward, leaving the pre-existing account in place.
- Selected account ID remains transient session state by existing design; cold start therefore continues to resolve the first visible account from persisted order rather than introducing a new selected-account persistence policy in this slice. This keeps Sort/reorder limited to `sortOrder` while making the restart guarantee specifically about durable ordering. S02 remains `L2/L3 partial` pending independent aggregate review.

## Stage 4 S02 aggregate maturity review — 2026-09-14

- Review target: merged `main@4e9101b13cbf26e96e7454192fd34884150c86d9`. Its tree is byte-for-byte identical to final PR #41 head `22a61192ea0bf91b6cf62dafb1d93fcc9a8586b6`, whose CI #365, Realm Integration #168, Native Android Gate #220 and Native Apple Gate #215 all succeeded. Current-main targeted S02 verification passes **7/7 suites, 32/32 tests**; full `npm run check` passes **62/62 suites, 321/321 tests**, ESLint remains **0 errors / 23 warnings**, provenance is **363 files / 0 unapproved donor collisions**, locale is **125 keys**, and the Realm integration suites pass **26/26** when run with the arm64 Node runtime matching the installed Realm prebuild.
- Cross-sub-slice composition is coherent: rename mutates only `label + updatedAt`; sort mutates only ordering metadata; hidden accounts retain global order while Home filters them; a still-visible selected account survives reorder; hide/delete reconciliation chooses the next prior-order visible account; delete remains blocked by unresolved submission and by the final-visible-with-hidden invariant; shared signers remain until the final reference is removed; final-total-account delete still returns through the existing onboarding bootstrap. No new aggregate logic defect was found in these merged behaviors.
- **Maturity decision: keep S02 at `L2/L3 partial`; do not promote the whole row to L3 or L4 yet.** The outstanding functional gap is the `select` part of S02. The Stellar reference has a visible-account switch/select overlay that permits choosing a specific account and persists the default account. Fresnica currently stores `selectedAccountId` only in `MainTabsNavigator` state, initializes it from the first visible account, and tapping the Home account summary merely cycles with `nextVisibleAccountId()`. `docs/stellar-source-parity.md` still records the account picker as `BEHAVIOR_MISMATCH`. This difference needs an explicit final Adapt decision plus direct-selection UX and selected/default-account persistence semantics before the S02 row can be signed off as fully L3.
- **L4 evidence is also incomplete.** The Android manual evidence proves rename, lifecycle, and sort in separate accepted sub-slices, but there is no single current-main aggregate flow covering list → direct select → rename → reorder → hide/restore → delete → restart. There is no iOS S02 product/UI flow evidence. The current product-flow harness accepts only `android` and exercises onboarding/balance/send rather than S02; Native Apple Gate proves build plus FresnicaCore/Realm runtime smoke, not account UI behavior; no CI job currently requires the S02 product flow.
- §7/§9 presentation completion also remains partial: account list/detail still contain hard-coded English product copy, and rename/hide/delete do not yet have the same explicit accessibility/dynamic-font/screen-reader-order acceptance depth that Sort gained during review. These do not invalidate the merged sub-slices, but they block an L4 claim for the aggregate feature.
- Minimal closure plan: first ship an isolated **S02 selection/picker** slice that resolves direct selection and persistence policy without reopening frozen Rename/Hide/Delete/Sort semantics; then run one aggregate Android + iOS product-flow acceptance on the resulting main tree and promote to L3/L4 only from that evidence. The separate repository-wide `format:check` coverage debt remains an engineering-quality PR and must not be mixed into S02 behavior.

## Stage 4 S02 direct-selection/default-account evidence

- Independent branch `stage4/s02-account-selection-2026-09-14` implements the remaining direct-selection product gap without modifying the frozen Rename, Hide/Delete or Sort/reorder implementations. The picker contains only visible accounts on the active network and reuses the shared `sortOrder → createdAt → id` order. Session `selectedAccountId` remains app-owned rather than moving into Feature or Account Capability. The approved aggregate review above is preserved from PR #42 commit `3274872e1b3f77457ecc4d8c313fd673f5735270`; PR #43 includes that commit as branch history so the intended merge order is **PR #42 first, then PR #43** without duplicating the evidence after #42 reaches `main`.
- Realm schema v4 adds `DefaultAccountPreferenceEntity`, keyed by `networkId`, for the public default account id and `updatedAt`; Account records gain no default field. Explicit selection validates the target, persists the network default first and only then updates session state. A failed explicit write therefore leaves the current account unchanged, keeps the picker open and exposes localized passive error text plus one proactive accessibility announcement. Automatic hide/delete reconciliation keeps a valid session fallback and retries persistence without changing the frozen lifecycle capability. Invalid persisted defaults normalize to a current-network visible account; restoring a hidden account does not reclaim default status; final-total-account onboarding clears that network preference.
- Independent-review P2 closure `09b0e8bc236d878685432c197564c43248529e5b` handles the previously unmodeled **zero visible accounts on the active network** state without changing Hide/Delete policy. `resolvePreferredVisibleAccountId` now returns no selection instead of throwing, and `MainTabsNavigator` routes that state to the current-network Settings/Accounts recovery stack. Only records from the active network are exposed there: hidden current-network accounts can be restored, an empty current-network set can add an account, and a visible account from another network is never adopted as the active selection. A navigation regression test covers this render path.
- Implementation/native target `fa315e0d8a8eefe68347de6365146557f4b0bd60`: clean detached-worktree `npm run check` passed **64/64 suites, 339/339 tests**, ESLint **0 errors / 23 warnings**, architecture/alias gates passed, locale dictionaries share **129 keys**, and provenance scanned **370 files with 0 unapproved exact donor blobs**. Targeted source Prettier and `git diff main...HEAD --check` passed. Realm integration passed **2 suites / 28 tests**, including v2→v4 and current v3→v4 migration, network isolation, close/reopen, same-network replacement and independent clearing. Remote **CI #368**, **Realm Integration #171**, **Native Android Gate #223** and **Native Apple Gate #218** all succeeded on this exact implementation head.
- Android emulator acceptance on 2026-09-14 used `emulator-5554` (`sdk_gphone64_arm64`, API 36) with existing visible accounts `11` and `PickB`. Opening the Home account picker showed both accounts and direct-selecting `PickB` changed Home from `Current account 11` to `Current account PickB`; force-stop/restart restored `PickB`. Hiding current `PickB` through the already-accepted lifecycle UI reconciled Home to `11`, and another force-stop/restart restored `11`. Restoring `PickB` made both accounts visible again, but a final restart still selected `11`, proving restore does not steal the persisted default. The pre-existing account state was preserved; no disposable account deletion was needed.
- Narrow independent re-review subsequently passed with no new P1/P2. PR #42 merged first, PR #43 then synchronized the new `main`, reran the exact-head gates, and merged as `48b77acc580b8dd47ea7f68b581c5c49ca67d8fb`. At that merge point S02 correctly remained `L2/L3 partial`; the later dual-platform aggregate acceptance below is the evidence that supersedes that maturity state.

## Stage 4 S02 dual-platform aggregate acceptance — 2026-09-15

- Acceptance target is merged `main@48b77acc580b8dd47ea7f68b581c5c49ca67d8fb`; no account product semantics were changed during acceptance. Android used `emulator-5554` (`sdk_gphone64_arm64`, Android 16 / API 36) and process restart was `adb shell am force-stop` followed by launcher start. iOS used iPhone 15 Pro simulator `DEF9E99C-0D02-4803-B457-CFB631433DFD` on iOS 17.2 with Xcode 16.2; restart was `XCUIApplication.terminate()` / `launch()`.
- Android aggregate flow passed `list → direct select → restart/default restore → rename → reorder → hide/restore → delete → restart`. Direct select changed both Home session and the Testnet `DefaultAccountPreferenceEntity`; force-stop restored the same account. Rename persisted, reorder retained the current account, hiding the current default selected and persisted the stable fallback, restart retained that fallback, restoring the hidden account did not reclaim default, delete cancellation/system-back stayed local, the last-visible guard remained enforced, deleting the true final account entered existing Onboarding, and Realm then contained zero Account / Account-Signer Reference / Signer / DefaultAccountPreference records; another restart stayed in Onboarding.
- iOS passed the same aggregate behavior on the exact target, split across small XCUITest cases rather than one monolithic harness: direct select/default restart, rename, reorder-with-current-account retention, hide fallback + restart, restore-without-default-steal, delete cancellation, last-visible guard, final delete → Onboarding → restart, modal close, and edge-swipe/back all passed. Realm snapshots independently confirmed the selected default, fallback default, restore-no-steal state, and final preference/account cleanup.
- Zero-selection recovery is production-validated on both platforms. With all active-Testnet accounts hidden while a Mainnet account stayed visible, neither platform adopted the cross-network account; the current-network Accounts recovery path exposed hidden records and Restore. With the active network containing **zero accounts** while a Mainnet visible account still existed, both platforms showed the current-network zero-account recovery state, excluded the Mainnet record, exposed `Add account`, and reached the watch-only Add screen.
- Current-main guard regression also passed: targeted selection/lifecycle suites **3/3, 27/27 tests** and Realm integration **2/2 suites, 28/28 tests**, covering unresolved `submitting`/`uncertain` delete blocking, final-visible policy, shared-signer reference-count cleanup, selection normalization and durable Realm behavior. Chinese locale was exercised with Android `font_scale=1.5` and iOS Dynamic Type `accessibility-large`; navigation/modal semantics remained usable. However Accounts/Account Detail still expose hard-coded English strings such as `Accounts`, `Identity`, `Network`, `Visibility` and `Add account`. TalkBack and VoiceOver were actually enabled and traversal gestures were exercised, but the automated focus capture was not reliable enough to sign screen-reader order. The standard remote native gates also do not yet require this aggregate S02 UI flow.
- **Maturity decision: promote S02 to L3 and record `L3 / L4 partial`; do not claim L4.** Production wiring and the cross-sub-slice account lifecycle are now proven on Android and iOS. L4 remains blocked by the localization debt, a trustworthy TalkBack/VoiceOver ordering acceptance, and making the aggregate UI/native evidence a repeatable required gate. After acceptance, emulator accessibility services, enlarged text settings and app data were restored to their pre-acceptance state.

## Stage 4 S05 Testnet Friendbot sub-slice — 2026-09-16

- Code target `373912dc0a1753692436b747b7b52c5493d48cc5` builds on merged `main@cb54987c0a053d88f3500d4b5d9edaabbdd9fc30`. The behavior spec is clean-room and fixes the product boundary before implementation: Friendbot is offered only for inactive Classic accounts on `stellar-testnet`; watch-only and protected-software accounts are both eligible because only the public account address is required.
- Network Capability owns the fail-closed eligibility policy. The Platform adapter sends only the public address to configured Friendbot, treats every non-2xx result as failure and never projects remote response bodies as product copy. Home de-duplicates in-flight requests per account, reloads the existing Balance/Horizon authority after success, preserves a localized retryable inactive state after failure, and ignores a stale completion after the selected account changes. No local balance, signer material, passphrase, mnemonic, XDR or transaction-recovery record is fabricated for Friendbot.
- Unit coverage includes Testnet/Mainnet/network/account-kind policy, HTTP success/failure encoding, inactive-panel eligibility, duplicate-disabled state and retryable localized failure copy. Local `npm run check` passes **68/68 suites, 353/353 tests**, ESLint **0 errors / 23 warnings**, provenance **382 files / 0 unapproved exact donor blobs**, and locale **138 keys**. `npm run test:realm` passes **2/2 suites, 28/28 tests** under an arm64 Node matching the installed Realm prebuild. A real macOS Node protocol smoke used a disposable random Testnet address and observed Friendbot HTTP 200 followed by the same Horizon account with native balance `10000.0000000` XLM.
- Android native acceptance is signed for the Friendbot sub-slice on a clean disposable `Fresnica_S05_Acceptance` AVD (`emulator-5556`, Android 16 / API 36). A fresh watch-only Testnet address first rendered the inactive Home with enabled Friendbot and Refresh actions; one tap changed Friendbot to disabled `Funding with Friendbot…`, then the inactive panel disappeared and the existing Balance/Horizon path rendered `XLM 10000.0000000`. A direct Horizon read of the same public address returned the same native balance, and force-stop/relaunch preserved the account while reloading that authority. A second inactive watch-only account was funded and, while that request was in flight, the user switched back to the first account; Horizon later reported the second account at `10000.0000000` while Home remained on the first account, proving stale Friendbot completion did not replace the newly selected identity or surface the second account on the first account Home. Selecting the second account afterward rendered its authoritative XLM balance. The pre-existing `Medium_Phone` / `emulator-5554` instance separately exhibited an AVD network/resolver corruption; a clean AVD resolved and launched the same APK, so that old-instance fault is retained only as environment debt.
- **Maturity decision: the Friendbot sub-slice is production-wired and Android-native accepted at L3, while aggregate S05 remains `L3 partial`.** Do not promote S05 to L4 from Android alone. The next acceptance unit is the Home/Balance aggregate closure plus repeatable iOS product-flow evidence and a required gate.

## Stage 4 S05 Home/Balance dual-platform aggregate acceptance — 2026-09-17

- Acceptance target is merged `main@ab794fa78829a66c403a9c7f00e131d35e350ca3`; no S05 product code or behavior changed during aggregate acceptance. The accepted tree already contains the L3 Friendbot sub-slice, shared Home focus revalidation, manual refresh/error recovery, and `asset-details`. Test fixtures created three disposable Testnet identities in the normal app Realm: a protected-software active account, a funded watch-only read-only account, and an unfunded watch-only inactive account. Friendbot was used only as fixture infrastructure for the first two addresses; product Friendbot UI was not re-exercised. Horizon reported native `XLM 10000.0000000` for both active identities and `inactive` for the unfunded identity.
- Android used a disposable `Fresnica_S05_Aggregate_20260917` Android 16 / API 36 AVD (`emulator-5556`) while the pre-existing `Medium_Phone` / `emulator-5554` remained untouched. Production Home rendered the protected account at `XLM 10000.0000000` with signing actions enabled; `asset-details` independently reloaded the same account/network balance. While on Asset Details, airplane mode was enabled only on the disposable AVD; returning to Home produced `Balances unavailable / Try again`, proving focus revalidation did not keep the cached balance. Restoring network and pressing `Try again` returned to `10000.0000000`. Production account switching then showed the funded watch-only account at the same balance with signing/Add-asset actions disabled, and the unfunded watch-only account in the honest inactive panel with Friendbot/Refresh entry present but not invoked. A Home pull-to-refresh also returned the same authoritative balance.
- iOS used a disposable iPhone 15 Pro simulator on iOS 17.2 with a fresh signed Debug build from the same main target. The first fixture launch stopped before persistence with `fixture-friendbot:400` because the Android run had already activated the reused Testnet addresses; the fixture was made idempotent for HTTP 400 and still required Horizon `active` before continuing. Production Home then rendered the protected account at `XLM 10000.0000000`, and production Asset Details independently rendered the same balance, public account and `stellar-testnet`. For error recovery, a temporary uncommitted transport fault injector was placed only in the test entry: the first `fetch` version was discarded after proving it did not affect Stellar SDK's axios/XHR Horizon transport; the corrected XHR-only injector made production Home render `Balances unavailable / Try again`, and disabling the fault followed by one `Try again` restored `10000.0000000`. The injector was removed and `index.js` restored byte-for-byte to main before the remaining checks. Read-only and inactive defaults were then selected only as fixture setup through the existing production default-selection helper, followed by cold launches of the original product entry: read-only rendered `10000.0000000` with signing/Add-asset unavailable, while inactive rendered the not-activated panel with Friendbot visible but untouched.
- Scope/evidence boundary is explicit. Android runtime directly proves the `Asset Details → Home focus` invalidation failure/recovery path. iOS runtime proves the same Home error/retry state machine and authoritative reload, but the focus-return fault itself was not repeated after the Simulator automation layer became unstable. The focus wrapper and Home implementation are shared React Native code, so this is sufficient for L3 production acceptance but not a reason to claim complete L4 cross-platform E2E depth. After acceptance, the disposable Android AVD and iOS simulator, temporary transport/select fixtures, screenshots and Metro process were removed; the prior iPhone 15 Pro simulator was restored to its previous Booted state, and the worktree returned to only the four pre-existing unrelated untracked architecture documents.
- **Maturity decision: promote aggregate S05 from `L3 partial` to `L3 / L4 partial`; do not claim L4.** Home/Balance availability, authoritative active/read-only/inactive states, manual/focus invalidation, retry/error recovery, Asset Details and the already-accepted Testnet Friendbot slice are production-wired with current Android+iOS product evidence. L4 remains open because this aggregate flow is not a repeatable required CI/E2E gate, iOS did not independently repeat the focus-return fault path, and trustworthy S05 dynamic-font plus TalkBack/VoiceOver focus-order acceptance is not yet closed.

## Stage 1 maintenance — Fresnica Native SDK 0.3.1 — 2026-09-16

- Base is merged `main@913b92ca028a7ffab10eed2791f3305680c2efaa` after S05 PR #45. Implementation target `e6f4098bdcfcf1700fc1546120dfce3ff97214bf` upgrades only published Native SDK consumption, compatibility pins and dedicated native gates; `66b41c67ffe6efdb4b3771efd208e5f425ab5976` changes only the Android smoke launcher from nondeterministic `monkey` to the app's declared `.MainActivity`.
- Upstream source of truth is `native-sdk-v0.3.1@984a741ab49ed5ca3eeab6da525bcacac5dd5d04`. Official SHA-256 is `2acbeb065d33abea787b0877674f6f263ea44daa9cc9a032942d46c98e0c88bb` for the Android AAR and `97545f6b5a3ab8b4fe05f0c8b7e012b4c76e9858500c373eea43fd21daa8d5be` for the Apple zip. The release's own adapter verifier accepts the local generated artifacts as RN 0.87.0 / Native 0.3.1 / Binding API 3 / adapter source 0.3.0.
- Local gates: `npm run check` **68/68 suites, 353/353 tests**, ESLint **0 errors / 23 warnings**, provenance **382 files / 0 unapproved exact donor blobs**, locale **138 keys**; Realm integration **2/2 suites, 28/28 tests** under a matching arm64 Node. iOS fresh Xcode rebuild + runtime returns `FRESNICA_PARSE_ACCOUNT_SMOKE_OK`; Android standard `npm run smoke:android -- --rebuild` on a clean disposable Android 16 / API 36 AVD returns the same marker. Both callbacks include Realm `ok`, Classic parse, `invalid-input`, external-signing bridge and SEP-53 bridge evidence.
- At this SDK-maintenance target, adapter revision `984a741...` had not yet exported native-only `verifyProtectedSignerPassphrase`; Mobile correctly kept Existing-wallet protected-signer creation fail closed rather than adding a private bridge. The later S01 prerequisite below advances the canonical adapter pin and removes that historical blocker without changing this maintenance result or promoting Stage 4 maturity. Remote PR gates remain separate evidence and must be recorded against the PR's exact final HEAD.

## Stage 4 S01 protected-signer passphrase verification prerequisite — 2026-09-16

- Base is `main@2d44dcac97472da83b7b8b0e5ea94383d983c241` after SDK 0.3.1 PR #46. Mechanism code target `32c16852d9d57575234ece9119d20297e243e654` keeps Native SDK 0.3.1 and adapter source package 0.3.0, but pins canonical adapter revision `c5eae08e84b197d534a05f02ae1a230a1e245f28`.
- Mobile adds one high-level boolean verification operation to `FresnicaSdkPort`, the required Native module contract and `ReactNativeFresnicaSdk`. It accepts only protected envelope + App Passphrase + expected signer public key. No unlock-key, mnemonic, secret, raw signer or private adapter method enters the Mobile contract.
- Canonical regeneration is independently evidenced by the upstream manifest verifier and generated binaries: Android adapter SHA-256 `22b21406d108f23d65e7c2bd67d960c66561b77dbecf53ed5c28fb97bb190268`, Apple adapter SHA-256 `ba13d945aa76e22e135bd603d0c83d5f619cd73d66749db207c58cf8b41692a2`. These consumer-built hashes record this local validation output; they are not release digests and may change with toolchain/build metadata while the pinned source/contract remains the same. The Android generated class and Apple canonical export both contain `verifyProtectedSignerPassphrase`.
- Local code gates are **68/68 suites, 355/355 tests**, ESLint **0 errors / 23 warnings**, provenance **382/0**, locale **138 keys**, Realm **28/28**. iOS and a disposable Android 16 / API 36 AVD both return `FRESNICA_PARSE_ACCOUNT_SMOKE_OK`; correct passphrase verifies, wrong passphrase maps to `invalid-passcode`, signer mismatch to `identity-mismatch`, malformed envelope to `invalid-protected-data`, and the boolean verification path does not expose sensitive material or mutate Realm/envelope. Mobile wrapper tests prove it does not call System Auth initialize/register/remove or Reveal.
- **S01 remains `L3 partial`; no maturity promotion.** The upstream safety blocker is removed and the frozen Existing-wallet behavior specification now has a production Create slice at `0eb20f7...`. Import duplicate/watch-only upgrade behavior, HD additional-account policy and aggregate Android+iOS acceptance remain separate follow-up slices.


## Stage 4 S01 Existing-wallet Create slice — 2026-09-17

- Behavior is frozen in `docs/behavior-specs/s01-existing-wallet-add-account.md` at `8e25f08...`; implementation code target is `0eb20f73bf036b5bcd89bda537308bea136be68d`.
- Existing-wallet Add Account exposes Create plus the existing watch-only path. Create verifies all unique protected signer verification targets before any generation/write; a wrong current App Passphrase leaves Account/Signer/default unchanged. A wallet with no protected signer establishes the first App Passphrase through the existing new-protection policy.
- Generated Account+Signer+reference use the existing atomic repository operation with mnemonic backup `pending`. The original slice treated explicit backup acknowledgement as the gate for selecting the new network default/current; the 2026-09-18 hardening below supersedes that product assumption with required mnemonic verification. Default-write compensation to `pending` remains unchanged.
- Existing System Auth is post-persistence only. Status lookup or signer registration failure preserves the durable account and surfaces `repair-required`; Mobile does not create a second security truth.
- Local code gates on the exact tree are 72/72 suites, 376/376 tests, ESLint 0 errors / 21 warnings, provenance 389/0, locale 165 keys, plus Realm 28/28. The shared native runtime smoke now includes the Create/restart/default chain and installs the same RN runtime polyfills as the established product-flow carrier before constructing AppServices. The first Android attempt exposed that missing carrier initialization before any Create assertion (`Horizon` module load); after the carrier-only fix, exact code commit `0eb20f73bf036b5bcd89bda537308bea136be68d` passed on disposable `Fresnica_S01_Create_Acceptance_20260917` / API 36 / `emulator-5556` with wrong-pass=`invalid-passcode`, pending restart recovery=`ok` and default restart restore=true. Native Apple Gate executes the same carrier on a simulator; Native Android Gate remains adapter/link/release-signing evidence only and is not counted as emulator evidence.
- This closes only the Create slice. Import, HD additional-account and aggregate S01 acceptance remain pending, therefore maturity stays `L3 partial`.

## Stage 4 S01 Existing-wallet Import slice — 2026-09-17

- Frozen Import behavior target is `2867dce96d22c2cddf597346913f660c799d1e2a`; atomic watch-only repository target is `c49b576a44f3a6ad1a70ad0cf7b6061652bab215`; Import Capability/shared protection orchestration is `260b10717e84be1aa59e0aa388bee303ce4ed1f7`; reachable UI/navigation/localization is `2824313894c8bd97d57db0343459c8c086d7c44e`; exact code/runtime target is `e8405af9a1c500787f45a2c99599934deba35b20`.
- Mnemonic and Stellar `S...` Import reuse the Create current-passphrase and post-persistence System Auth helpers. All unique protected targets verify before protection/write; imported mnemonic forwards language/passphrase/index; both imported signer kinds persist `backupState=not-required`.
- Same-network visible watch-only identity is upgraded in place by one repository transaction that creates only Signer + reference and preserves Account metadata. Already-signed duplicate is `account-already-exists`; hidden watch-only is `account-not-selectable` rather than an implicit S02 visibility mutation; cross-network Account identity remains independent.
- Default/current uses the existing network preference flow: durable Import is visible to the account list first, then default persistence gates current selection. A default write failure keeps the previous current/default authoritative. Sensitive recovery material stays component/Capability-local and is absent from navigation/log/error/default metadata.
- Exact code-head gates are 74/74 suites, 397/397 tests, ESLint 0 errors / 21 warnings, provenance 394/0 and locale 191 keys. Realm is 31/31 on the same product tree before the deterministic native-smoke-fixture-only amend. Disposable Android API 36 acceptance on exact `e8405af...` returns `FRESNICA_PARSE_ACCOUNT_SMOKE_OK` with wrong-pass zero-write, watch-only upgrade, duplicate rejection, no pending-backup and restart/default restore. The disposable AVD was removed and `Medium_Phone` remained untouched. Final exact-PR-head Apple runtime evidence is supplied by Native Apple Gate; Native Android Gate remains build/link/signing evidence, not emulator evidence.
- This closes only Import. HD additional-account and aggregate dual-platform S01 acceptance remain pending, so maturity stays `L3 partial`.

## Stage 4 S01 HD additional-account slice — 2026-09-17

- Frozen HD behavior target is `281bdb4c3fc34656971963acd4ffb153fc6de150`; Capability/provisioning target is `0be72adcf9ad89580ad9c7a5fefb2fefa2ed6b50`; reachable Add Account UI/localization/default-selection target is `ee26c85b24f709fa952e754e6772954b6d67ee4b`; exact code/runtime target is `b33baae27c9ff680aaff907566fbeb0ba02772aa`.
- Eligible sources are only mnemonic-backed protected signers referenced by visible current-network Accounts with `backupState=confirmed|not-required`. Pending backup, secret-backed, missing-envelope and hidden/unreachable sources fail closed. UI state contains only public source signer identity and the explicit index; Capability reloads the opaque envelope from Repository immediately before derivation.
- Mobile calls canonical `deriveMnemonicSigner` only after the existing all-unique-protected-target App Passphrase verification succeeds. It does not Reveal mnemonic material, parse opaque envelopes or add a Recovery Source persistence model. The confirmed index is explicit and constrained to Stellar's `0..2^31-1`; UI displays SEP-5 `m/44'/148'/index'`. Derived backup state inherits the source (`confirmed` or `not-required`) and never becomes pending.
- Same-network visible watch-only upgrade, already-signed duplicate rejection, hidden-account rejection, post-persistence System Auth registration/repair, and default-before-current selection reuse the Import/Create orchestration. A default write failure leaves the previous current/default authoritative while preserving the durable derived Account+Signer graph for later explicit selection.
- Exact code-head gates are 76/76 suites, 414/414 tests, ESLint 0 errors / 21 warnings, provenance 398/0 and locale 206 keys; Realm is 31/31. Disposable Android API 36 acceptance on exact `b33baae...` returns `FRESNICA_PARSE_ACCOUNT_SMOKE_OK` with wrong-pass=`invalid-passcode`, no pending backup and restart/default restore. The disposable AVD was removed and `Medium_Phone` remained untouched. Final exact-PR-head Apple runtime evidence must be supplied by Native Apple Gate; Native Android Gate remains build/link/signing evidence rather than emulator evidence.
- This closes the HD implementation slice locally/Android. Aggregate dual-platform S01 acceptance remains pending, so maturity stays `L3 partial`; PR C must not merge without the exact-head Apple/native/CI gates.

## Stage 4 S01 dual-platform aggregate acceptance — 2026-09-17

- Acceptance target is merged `main@a2b17eba8285c57b4b56f3c3cec5e817dd184e95` (PR #50 merge; parents `4849fcf98c0ee2301e68d5bde6eb0563150a1683` + `44775245c3d087b5194c572fb209b9d4bbb5a0de`). No S01 product semantics changed during aggregate acceptance. PR #50 final head had CI #388, Realm Integration #191, Native Android Gate #243 and Native Apple Gate #238 all successful; the Apple gate also completed its simulator FresnicaCore + Realm runtime smoke.
- A temporary, uncommitted acceptance carrier called the production `createAppServices`/S01 Capability paths against one isolated Realm and one wallet lifecycle: existing watch-only -> Create -> pending mnemonic backup -> close/reopen -> recover/confirm backup -> establish System Auth -> wrong current passphrase zero-write -> Import secret into a same-network watch-only account in place -> duplicate rejection -> HD derive -> persist default -> close/reopen -> remove one signer System Auth enrollment -> observe enrollment gap -> repair through the existing Security Capability. The carrier was deleted after acceptance and is not counted as a standard CI gate.
- Both platforms returned `FRESNICA_PARSE_ACCOUNT_SMOKE_OK` with `accountCount=4`, `signerCount=3`, the original watch-only Account preserved, pending-backup restart recovery=`ok`, wrong-pass reason=`invalid-passcode`, watch-only upgrade=`ok`, duplicate reason=`account-already-exists`, no pending backup after Import/HD, final default restored after restart, post-persistence System Auth registration=`ok`, a deliberately created signer-enrollment gap observed, repair=`ok`, and final enrollment `3/3` protected signers.
- Android used disposable `Fresnica_S01_Aggregate_20260917` (`emulator-5556`, Android 16 / API 36). A fingerprint was enrolled on that AVD and the aggregate domain initialization reached a real `BiometricPrompt`; one fingerprint event produced a strong-biometric success before the lifecycle completed. Two earlier attempts failed before any product assertion because the acceptance carrier lost ADB reverse during emulator reconnection and then used host Metro `8082` while RN debug resolved `10.0.2.2:8081`; the final run used the repository's established host `8081` routing. The disposable AVD was deleted afterward and `Medium_Phone` / `emulator-5554` was not used or modified.
- iOS used the iPhone 15 Pro simulator (`DEF9E99C-0D02-4803-B457-CFB631433DFD`, iOS 17.2) with a fresh **normally signed** Debug simulator build from the exact acceptance target. An initial diagnostic build with `CODE_SIGNING_ALLOWED=NO` could not access the System Auth Keychain (`-34018`) and was discarded before product acceptance; the signed build passed `codesign --verify`, Native `canUseSystemAuth=true`, and a clean-domain probe before the aggregate run. The same production System Auth domain/enrollment/repair path then completed successfully. A visible Face ID prompt was not separately observed and is not claimed as evidence. After acceptance, a Native reset probe proved `before=true -> removed=true -> after=false`, and the aggregate Realm files were removed.
- **Maturity decision: promote S01 from `L3 partial` to `L3 / L4 partial`; do not claim L4.** Create, Import and HD are user-reachable and their cross-slice same-wallet lifecycle is proven on Android and iOS. A later 2026-09-18 review found one additional semantic gap in the generated-mnemonic backup surface: acknowledgement was not verification. The hardening slice below must close that gap before the prior statement that only L4 gate/accessibility debt remains is considered current. Reveal/Export, passphrase rotation, generic app-session System Auth unlock and hardware-wallet work remain separate product IDs/slices rather than S01 aggregate blockers.


## Stage 4 S01 mnemonic backup verification hardening — 2026-09-18

- Post-aggregate review found that all generated-mnemonic Create surfaces displayed the recovery phrase and then allowed a one-tap acknowledgement to mark `backupState=confirmed`; no word/order proof was required. This is a real S01 product-semantic gap, not an aggregate-navigation defect.
- Behavior is frozen in `docs/behavior-specs/s01-mnemonic-backup-verification.md`. First-run Create, Existing-wallet Add Account → Create and pending-backup process recovery now share one in-memory verifier: hide the phrase, request three distinct word positions, allow mismatch correction, and invoke backup completion only after all requested words match.
- Process-death security ownership is unchanged: Realm persists only protected envelope + public pending metadata; fresh-App-Passphrase Fresnica Core `reveal` reconstructs the mnemonic into current UI memory, and challenge indices/answers are never persisted or placed in navigation/log output.
- Existing default/current ordering and compensation remain unchanged. Existing-wallet Create still switches default/current only after verified backup completion; a default write failure returns backup state to `pending`.
- Local current-tree verification: static gates pass with ESLint 0 errors / 21 existing warnings, provenance 400 files / 0 unapproved exact donor blobs, locale 3 dictionaries / 215 keys; full Jest is 79/79 suites and 426/426 tests; Realm is 2/2 suites and 31/31 tests under a SHA-256-verified temporary official Node 22.14.0 arm64 runtime. A fresh Android debug build from this worktree also succeeds.
- Disposable Android 16 / API 36 runtime acceptance used the production `MnemonicBackupVerification` component through a temporary non-committed entry. The initial phrase presentation showed all 12 test words; `Verify backup` then hid the complete phrase and produced three distinct requested positions (`Word 3`, `Word 6`, `Word 9`) with no expected words present in the challenge labels. Remote-command safety correctly blocked automated recovery-word text injection, so this run does not claim Android keyboard wrong→correct completion. That retry behavior is instead directly covered by the executed state/UI tests, including mismatch → edit clears stale error → corrected submit succeeds. The disposable AVD, temporary Metro resolver/entry and local artifact links were removed afterward; `Medium_Phone` was not modified.
- This hardening corrects the prior conclusion that no additional S01 product semantics remained. It does not promote S01 to L4; repeatable aggregate gating and trustworthy dynamic-font/TalkBack/VoiceOver focus-order acceptance remain separate L4 work.

## Stage 4 S30 Trustline product hardening specification — 2026-09-18

- **Scope is presentation/product hardening only.** Preserve the existing Add / Set Limit / Remove capability semantics, exact-XDR review identity, asset-code case, reserve/fee and liability checks, issuer/authorization/clawback/LP-removal rules, pre-sign ledger revalidation, shared Signing policy and Stage 2.5 pending/reconciliation/duplicate guard. Do not refactor Capability, Payment, Signing, Transaction, XDR or recovery to satisfy this slice.
- **Load failure must be recoverable in-place.** A balance/trustline load transport failure renders a localized error state with an explicit retry control wired to the same authoritative `loadBalanceSnapshot` path. Inactive and unsupported-account states remain truthful non-retryable states.
- **The complete Manage Assets surface is locale-owned.** User-visible headings, labels, placeholders, helper text, review/result copy, actions and accessibility labels use the three existing dictionaries (`en`, `zh`, `zh-TW`). Runtime ledger identifiers, asset codes, issuer/source addresses, exact decimal values, transaction hashes and Horizon result codes remain data rather than translation keys.
- **Accessibility is explicit at the screen boundary.** Interactive controls expose button/text-input roles and meaningful localized labels, disabled state is exposed where applicable, async failures have one deliberate screen-reader announcement path without duplicate live-region speech, and review/result/status content remains readable under dynamic type without fixed-height text containers that would clip localized copy.
- **UI regression evidence is required before aggregate.** Screen-level tests must cover load-error → retry, localized/a11y control contracts, Add / Set Limit / Remove affordances, passphrase fallback presentation and result-state rendering. Static styles move from `ManageAssetsScreen.tsx` into same-directory `styles.ts`; this extraction must not alter product semantics.
- **Aggregate comes only after this hardening merges.** The later S30 aggregate must independently prove Android+iOS Add → Set Limit → Remove, App Passphrase fallback, System Auth success, real Horizon state, rejected/uncertain/restart recovery and Home invalidation refresh. The already-deferred real-iPhone Native System Auth cancellation evidence is not a new S30 prerequisite and remains the shared Stage 9/release acceptance debt.

## Stage 4 S30 dual-platform aggregate acceptance — 2026-09-20

- Acceptance target is merged `main@0e84fd5638010887a6687b371047452fa830fdd0`, the PR #59 merge whose tree is byte-for-byte identical to independently reviewed fix head `e377aa7cb61ad283ca09a8d1514d181f63a7a6d6`. PR #59 corrected only the Horizon adapter rule that an omitted `is_clawback_enabled` means `false`; asset identity and authorization fail-closed checks stayed unchanged. The earlier pre-fix `MiXeD` Add transaction is retained only as defect-location evidence and is not counted as final aggregate acceptance.
- Fresh iOS and Android Testnet sources independently ran production `createAppServices`, Trustline Product Flow, exact-XDR review, Realm pending state, Fresnica signing, Horizon and shared reconciliation. On both platforms Add preserved mixed-case `MiXeD`, first returned `passphrase-required`, then submitted with App Passphrase authorization, and the post-#59 production Horizon read successfully loaded the new trustline at limit `708269837873.6765000`. iOS Add hash is `bf90cb756c7958c343f460c2dfb47b20739d0f783cacc396fe33597f715165ca`; Android Add hash is `133544eec13a1403561ac9c55615b88d361d118cbb47c63ba782902f6fbaa64e`.
- Both platforms then enabled the production System Auth domain and submitted Set Limit through `authorization=system-auth`. A real buying liability of `5.0000000` blocked a requested limit of `4.0000000` as `trustline-limit-below-commitment`; after clearing the offer, limit `100.0000000` was confirmed by Horizon. iOS Set Limit hash is `84ac1d190fca1cefca942fa11c8beda6c9e31dc7cf0902583e0ea36c98b29fc0`; Android is `324013c30cf3ac7e828d10c9d87eee44dafc3cdacf9770417b677f90b0be8ecb`. A deliberately stale reviewed XDR was deterministically rejected as `tx_bad_seq` on both platforms without mutating the confirmed limit.
- Remove stayed fail-closed for a real non-zero issued balance and for a real held liquidity-pool share, then succeeded after both conditions were cleared. The LP path used actual Testnet pool deposit/withdraw fixture transactions while the product Remove itself stayed on the production Trustline path. iOS Remove hash is `f913b3a96f7d0dd0e4a153f9fccd5fd2249c3b9bee66d1c31bd57ab79ae33142`; Android is `5bcfb2d263e2d890efa7b136fbf3bcc56e45b909279794950f42e887720066ac`. Horizon then reported `MiXeD` absent.
- A second fresh trustline `UnCt` exercised the existing test-induced uncertain carrier: Horizon accepted the real signed transaction while only the temporary transport projection returned `uncertain`. Realm durably stored that exact hash as unresolved, authoritative Horizon reads already showed the trustline, and a real process replacement/cold launch reconciled the same hash to `confirmed` without another sign or broadcast. iOS recovered `a6e1a93d7a0dd281684cfeb86e689bf4d536b118633cb8e3dba200de3b0fb28e`; Android recovered `b701e48b950524f91aee06f8938a8554ac5b2e37242e40033d7d6c99ebfdf85b`.
- Android additionally proved five cryptographic Native BiometricPrompt successes on a disposable API 36 AVD with an enrolled fingerprint; iOS independently completed the same System Auth success paths with the disposable iPhone 15 Pro / iOS 17.2 simulator and enrolled Face ID. The previously deferred real-iPhone System Auth cancellation observation remains Stage 9/release debt and is not reclassified as an S30 blocker.
- Product-surface evidence used the restored production entry after transaction acceptance. Submitted/uncertain results incremented the ledger-read invalidation revision, and the restored Android Home then showed the real Horizon-backed `UnCt balance 0.0000000`; removed `MiXeD` did not reappear, so the acceptance does not rely on a local optimistic trustline list. A separate temporary presentation carrier rendered the production `ManageAssetsScreen` with the same App providers and production dependencies only: after the JS bundle was already running, an unreachable system proxy forced its real Horizon load into `Unable to load trustlines`; clearing the fault and pressing the screen's own `Retry` restored `UnCt`, balance `0.0000000`, limit `708269837873.6765000`, Set Limit and Remove in one retry.
- Production Settings changed locale to Simplified Chinese and the real Manage Assets surface rendered `管理资产 / 添加资产 / 资产代码 / 发行方 / 我的资产 / 设置限额 / 余额 / 限额 / 移除`. On Android `font_scale=1.5`, the same screen reflowed without losing its inputs or `UnCt` Set Limit/Remove controls. TalkBack was genuinely bound with spoken feedback enabled; the S30 screen exposed meaningful focusable labels including `资产代码`, `发行方`, `设置 UnCt 的限额` and `移除 UnCt 信任线`, and accessibility focus remained in the active Fresnica window across basic next-item traversal. The release TalkBack service does not expose its current node hierarchy, so exact focus ordering or exact spoken utterances are deliberately **not** claimed as closed; iOS VoiceOver/focus-order depth is likewise still open.
- Post-acceptance gates on the unchanged product tree passed: `npm run check` completed **82/82 suites and 457/457 tests** with zero lint errors, and Realm integration completed **2/2 suites and 31/31 tests** under an arm64 Node runtime matching the arm64 Realm native prebuild.
- **Maturity decision: promote S30 from `L3 partial` to `L3 / L4 partial`; do not claim L4.** The frozen Add / Set Limit / Remove, App Passphrase fallback, System Auth success, authoritative Horizon state, deterministic rejection, uncertain/restart recovery, Home re-read, load-error Retry, locale, dynamic-font and basic screen-reader evidence is now current on the post-#59 tree. L4 remains blocked by converting this aggregate into a repeatable required CI/E2E gate and by trustworthy platform-level screen-reader focus-order depth, especially iOS VoiceOver. No new Capability, Payment, Signing, Transaction, XDR or recovery semantics were introduced by acceptance.

## Stage 4 S07 dual-platform aggregate acceptance — 2026-09-18

- Product acceptance target is merged `main@a921635ed81c5d5557c90b5daac630ba5c65f682` (PR #53 merge). Aggregate acceptance executed against the unchanged merged product tree. Temporary carrier files were not committed and were removed after validation. The final evidence PR changes documentation only; no S07 product code, memo semantics, exact-XDR binding, signing policy, submission policy or recovery behavior changed during aggregate acceptance. PR #53 final head `89a8a8d...` had CI #399, Realm Integration #202, Native Android Gate #254 and Native Apple Gate #249 all successful.
- A temporary uncommitted aggregate carrier used production `createAppServices`, Realm, Fresnica Native SDK, Balance, `buildSendReview`, `submitSendReview`, Horizon/Testnet and the shared transaction reconciliation coordinator. It created one fresh protected source per platform, funded it through Friendbot as fixture infrastructure, exercised real Testnet writes, and deleted/restored all carrier files afterward. Callback output was restricted to public addresses, memo projections, statuses and transaction hashes; mnemonic, App Passphrase, protected envelope and XDR were not emitted.
- Android used disposable `Fresnica_S07_Aggregate_20260918` (`emulator-5556`, Android 16 / API 36) while `Medium_Phone` / `emulator-5554` remained untouched. Text review preserved semantic surrounding whitespace and the first authorization attempt returned `passphrase-required`; the second stage submitted with `authorization=passphrase`. ID reviews canonicalized `0007 -> 7` and `0008 -> 8`; the first real submission succeeded and the intentionally stale second XDR was deterministically rejected by Horizon as `tx_bad_seq`. Hash review canonicalized uppercase input to lowercase 64-hex and submitted with `authorization=system-auth`. A real System Auth cancellation returned Native `user-cancel`; retry remained usable and submitted through System Auth. A final real Horizon-accepted write was projected by the test-only transport wrapper as `uncertain`, persisted as unresolved, survived force-stop/relaunch, and the fresh process reconciled the same hash to `confirmed` without signing or broadcasting again.
- Android public transaction evidence includes Text `a13480de...`, ID submitted `d0e1a246...`, deterministic rejected `b9e1e430...` (`tx_bad_seq`), Hash/System Auth `7ea1071d...`, post-cancel retry `9f660bac...`, and uncertain/restart-recovered `8d69df34...`. The disposable AVD was deleted afterward.
- iOS used a disposable iPhone 15 Pro / iOS 17.2 simulator and a fresh current-tree normally signed Debug build. A stale DerivedData app that lacked `verifyProtectedSignerPassphrase` failed before product assertions and was discarded. The fresh build proved the same Text passphrase fallback, ID canonicalization plus real `tx_bad_seq` rejection, Hash System Auth success, real Testnet submitted outcomes, durable uncertain state and same-hash cold-start reconciliation to confirmed. Public evidence includes Text `28d02e19...`, ID submitted `438f6229...`, rejected `78551819...`, Hash/System Auth `5f718fa8...`, and uncertain/recovered `5e5a4642...`.
- The iOS simulator did **not** provide trustworthy native cancellation evidence. LocalAuthentication completed the System Auth signing path without a stable user-visible cancellation window; Escape, Home/backgrounding and simulated non-matching Face attempts did not produce `user-cancel`. The carrier therefore recorded an explicit `system-auth-cancel-gap` rather than faking success. Android native cancellation plus shared JS/native cancellation mapping tests remain valid, but they are not a substitute for an iOS-native cancellation observation.
- After cleanup, the disposable Android AVD and iOS simulator were deleted, the prior iPhone 15 Pro simulator was restored to its prior Booted state, `index.js` returned to the product entry, and the acceptance carrier/server/probe files were removed. Current branch guard then passed `git diff --check` and full `npm run check`: 81/81 suites, 447/447 tests, ESLint 0 errors / 21 existing warnings, architecture/alias/provenance/locale gates green, provenance 405 files / 0 unapproved exact donor blobs, and 3 locale dictionaries / 237 shared keys.
- **Maturity decision: keep S07 at `L3 partial`; do not promote to `L3 / L4 partial` yet.** Android closes the requested aggregate runtime matrix, and iOS closes Text/ID/Hash, App Passphrase fallback, System Auth success, submitted/rejected/uncertain and restart recovery on the current build. The remaining aggregate blocker is direct iOS-native System Auth cancellation/retry evidence, preferably on a real iPhone or another environment that exposes a cancellable prompt. Once that narrow gap closes, maturity can be reconsidered independently. L4 would still require a repeatable required aggregate gate plus trustworthy dynamic-font and TalkBack/VoiceOver focus-order/tap-driven UI acceptance.
- **Scheduling decision — 2026-09-18:** no real iPhone is currently available, so the missing iOS-native cancellation/retry observation is explicitly deferred to Stage 9 / release acceptance rather than simulated or inferred. This deferred evidence blocks S07 maturity promotion and final release sign-off only; it does not block S30 Trustline or S13 Activity work. Before any release candidate is signed off, a real iPhone must demonstrate `cancel → user-cancel → pending=0 → retry → submitted`. Payment, Signing, Transaction, XDR and recovery behavior must not be modified solely to satisfy this evidence gap.

## Current milestone security / release evidence

- S04 `6a19346`: reachable Security Settings now requires an explicit second confirmation before disabling System Auth; cancellation paths do not call Native remove, in-flight confirm is de-duplicated, and Native failure preserves the truthful enabled state. This slice used no donor implementation; provenance is `none / Fresnica security requirement`. S04 is therefore `L3 partial/blocker`, not L4, because app-session unlock and other security/privacy sub-behaviors remain incomplete or upstream-blocked.
- Release-critical S04 `50d8653`: Android release no longer references `signingConfigs.debug`; missing/partial credentials fail closed from the actual release task graph. A temporary local PKCS12 successfully signed `assembleRelease`, and `apksigner` verified `CN=Fresnica Local Release Gate` rather than Android Debug. No production keystore or password was created, persisted or committed.
- Final local milestone validation target `f49dc50`: `npm run check` 56/56 suites and 284/284 tests, `npm run test:realm` 17/17, ESLint 0 errors / 28 warnings, locale dictionaries 98 keys; fixed-commit provenance regeneration had zero ledger drift. Real Android Testnet recovery used exact hash `fc75bde872216a89d8fa867269a7ee2fd40d652d4a93e58f256d7c33c381fecc` across process replacement PID 8360 → 8525, and both Android/iOS standard native gates passed. Remote CI was not yet recorded because the branch remained 11 commits ahead of origin.
- Android Application ID remains `com.fresnica.mobile`, production release credentials are not provisioned, and iOS distribution identity is not frozen. Therefore Stage 9 remains incomplete even though the debug-signing fallback merge blocker is closed.
- After `b5a3be6` was pushed, Stage 0A target-tree audit found 21 exact donor presentation PNG blobs plus donor-port presentation helpers in the rewrite lineage. Direct merge of `rewrite/stellar-source-parity` to `main` is therefore prohibited even after those files are deleted from its tip: the historical blobs would remain reachable through the merge. The approved path is a clean integration branch from `origin/main` carrying only the final remediated tree, followed by full revalidation and a normal `integration → main` merge.
- Remediation evidence: `36d306f` removes the donor presentation trees and converts Shell/Home/Settings to neutral functional presentation; `bd916c5` adds the fixed full-tree collision/marker gate and Fresnica-owned debug key. On the remediated rewrite tip, `npm run check` is 56/56 suites and 284/284 tests, Realm is 17/17, ESLint is 0 errors / 23 warnings, and target-tree collision is 0 unapproved exact blobs with only the pinned RN 0.87 `.xcode.env` exception. These are pre-integration results only.

## Gate rules

- A Feature PR must name at least one stable `Sxx` ID or add a reviewed new ID before implementation.
- `Adopt` never means source-copy permission. Direct migration is separately controlled by `docs/provenance/donor-source-ledger.tsv` and an approval record.
- `Exclude` applies only to the explicitly excluded donor behavior; it does not remove adjacent Fresnica requirements.
- A row reaches L4 only with the execution-plan integration/native/E2E evidence, not by route/file existence.
- New behavior discovered under execution-plan §7, §10 or later audits receives a new stable ID rather than being hidden inside a Stage bullet.
- Stable IDs are append-only. Do not renumber existing rows when a missing behavior is discovered; S30 was added for this reason.
- Evidence must identify the exact commit, platform/device, command or scenario, date and result. A historical green run is not current-head evidence.
