# Donor Source Provenance Gate

This directory is the Stage 0A provenance record for Fresnica Mobile.

Audited donor snapshots:

- Stellar: `bd0f4540b2496a57314e4e29bc6fcf8ff691b60f`
- Xaman-App: `01e2538b08efa2c960233911fad02633f31ce6cd`

Both donor trees carry the XRPL Labs CUSTOM dual license. File presence in the Stellar derivative tree is not proof that Fresnica may directly migrate that source.

Default implementation policy:

- product behavior, protocol inputs/outputs, errors and acceptance criteria may be studied;
- implementation structure, expressions, comments, tests and resources are rewritten clean-room;
- a direct migration requires file-level independent-rights evidence plus an explicit approval record;
- absence of the same path in the audited Xaman commit is not proof of independent ownership.

`donor-source-ledger.tsv` is generated by `node scripts/audit-donor-provenance.mjs` and is the file-level evidence index. It proves which audited blobs were identical or different at the two pinned snapshots; it is not a legal conclusion and cannot prove independent authorship by itself.

## Current audited coverage

| Area | Files | Xaman inherited | Stellar modified / provenance-unproven |
| --- | ---: | ---: | ---: |
| freighter | 18 | 0 | 18 |
| local assets/icons/fonts | 1525 | 1024 | 501 |
| navigation | 5 | 0 | 5 |
| services | 56 | 6 | 50 |
| theme | 6 | 2 | 4 |
| Vault | 20 | 13 | 7 |
| XAppBrowser | 9 | 2 | 7 |
| xApps | 3 | 1 | 2 |
| **Total** | **1642** | **1048** | **594** |

`Stellar modified derivative` deliberately includes files that have no same-path counterpart in the audited Xaman commit when independent ownership has not been proven. That conservative classification prevents a newly-added file in a derivative repository from being treated as automatically safe to copy.

No file is currently classified as `provably Fresnica original` for direct migration. No direct-migration approval is recorded by this gate.

## Clean-room working rule

Before implementing behavior learned from a donor surface:

1. Record the user-visible flow, protocol inputs/outputs, state transitions, security invariants and acceptance cases in the product trace row or a linked behavior specification.
2. The implementation is written from that specification and Fresnica contracts, without donor source, tests, comments or local resources being used as a coding template.
3. Record donor paths only as audit evidence in the PR. Do not paste donor excerpts into implementation issues, code review or generated source.
4. Prefer a separate behavior reviewer and implementer. When staffing does not allow separation, keep a contemporaneous behavior specification and require an independent implementation review before merge.
5. A direct migration remains prohibited unless a separate rights review supplies file-level history, author/rightsholder confirmation, dependency licensing and an explicit approval record.

Regenerate the ledger only against the audited commits recorded above, or update this README and review the resulting ledger diff in the same PR. Moving donor `HEAD` must never silently redefine the audit baseline.

## Current target-tree collision gate

`donor-blob-index.tsv` is a full-tree hash index for the same fixed donor commits above. It is regenerated explicitly with `npm run provenance:index`; normal checks never follow donor `HEAD` and do not require the donor repositories to exist.

`npm run provenance:check` scans every tracked or non-ignored target file and fails on an exact Git blob match with either fixed donor tree by default. It also scans implementation source for known direct-port markers such as `Source port`, `source-equivalent`, donor implementation annotations and explicit Stellar/Xaman source-path comments. The command is part of `npm run check`, so the normal CI and native workflows enforce it automatically.

An exception is valid only when `docs/provenance/target-tree-collision-allowlist.json` binds one target path to its exact Git blob, SHA-256, third-party source repository/version/path and license proof. Stale or changed exceptions fail the gate.

The only current exception is `ios/.xcode.env`:

- target Git blob: `3d5782c71568d32eec2fe71b034efcde053305f9`;
- target SHA-256: `3c02dca07775b31ce4e77dfb3bb660d7c6e577474d65a6216f1ea9bef54e1ca6`;
- source: `react-native-community/template` tag `0.87.0`, `template/ios/_xcode.env`;
- source Git blob: the same `3d5782c71568d32eec2fe71b034efcde053305f9`;
- source package/version evidence: `@react-native-community/template` `0.87.0`;
- license: MIT, `LICENSE` blob `7042b1f8c786a8881bd37d6d2ce923d43cec4506`.

This is a path-and-content exception, not a general "template files are safe" rule. The Android debug keystore is deliberately not allowlisted; Fresnica generates its own debug key instead.

Passing this automatic gate is **not** proof that a donor-informed implementation is clean-room. Behavior specifications, provenance records and human review of implementation structure remain mandatory. In particular, Home, Settings, MainTabBar and other presentation surfaces must not preserve donor-specific component structure merely because their bytes differ.
